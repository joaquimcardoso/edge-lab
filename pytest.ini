[pytest]
# src-layout package (src/edgelab/...) isn't installed anywhere; this
# makes it importable for tests without needing a package install step.
# scripts/ holds runnable entrypoints (STORY-008) importable the same way.
pythonpath = src scripts
testpaths = tests
